# Distributed rate limit (BN-SEC-005 / U-013).
# Required in production for rate limits + web sessionVersion edge mirror.
# Optional locally (memory fallback allowed in non-production).
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
